# This file can be empty or can be used to make submodules available directly,
# e.g., from .models import Student
# However, to resolve the circular import, we are removing the import of '.routes'.
# The router from app.students.routes is imported directly in app.main.py.
